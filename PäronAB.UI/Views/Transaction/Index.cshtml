@model PäronAB.UI.ViewModels.TransactionViewModel

@{
    ViewBag.Title = "Transactions";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Add transaction</h2>
<form action="Transaction/Index" method="post">
    <table class="table">
        <tr>
            <td>Date</td>
            <td>
                <input id="date" name="Date" type="date" value="" class="form-control" required="required" />
            </td>
        </tr>
        <tr>
            <td>SKU</td>
            <td>
                <input id="sku" name="sku" type="text" value="" class="form-control" required="required" />
            </td>
        </tr>
        <tr>
            <td>Quantity</td>
            <td>
                <input id="qty" name="qty" type="number" value="" class="form-control" required="required" />
            </td>
        </tr>
        <tr>
            <td>Warehouse ID</td>
            <td>
                <select id="warehouseId">
                    @foreach (var warehouse in Model)
                    {

                    }
                </select>
                @*<input id="warehouseId" name="warehouseId" type="number" min="0" value="" class="form-control" required="required" />*@
            </td>
        </tr>
        <tr>
            <td><input type="submit" value="Save" /></td>
            <td></td>
        </tr>
    </table>
</form>

@Html.Partial("TransactionPartial");

